# Core — builds the .docx
python-docx>=1.1.0

# Streamlit UI
streamlit>=1.36

# Google Drive (Shared Drive access via service account)
google-api-python-client>=2.0
google-auth>=2.0

# Gemini engine (the app's FAQ writer)
google-genai>=0.3
